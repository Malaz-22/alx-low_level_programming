#include "main.h"

/**
 * add - adds two integers and returns the result
 * Return: result
 * @a:first num
 * @b:second num
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
