#include "main.h"

/**
 * print_last_digit - print last digit of number
 * @n: the number
 * Return: value of last digit
 */
int print_last_digit(int n)
{
	n = 0;
	while (n <= 9)
	{
		return (n);
		n++;
	}
}
