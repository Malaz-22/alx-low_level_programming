#include "main.h"

/**
 * _strcat - concatenates two strings
 * @dest: first string
 * @src: second string
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
	int i, n;

	for (i = 0; src[i] != '\0'; i++)
	{
	}
	for (n = 0; n <= i; n++)
	{
	}
	dest[i] = src[n];

	return (dest);
}
