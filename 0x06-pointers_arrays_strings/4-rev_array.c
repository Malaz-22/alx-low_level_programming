#include "main.h"

/**
 * reverse_array - reverses the content of an array of integers
 * @a: array
 * @n: num of elements
 */
void reverse_array(int *a, int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
	}
	for (i = n - 1; a[i] >= 0; i--)
	{
	}
}
