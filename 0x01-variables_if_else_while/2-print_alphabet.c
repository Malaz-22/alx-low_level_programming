#include <stdio.h>

/**
 * main - body of program
 * here code begins
 * Return: 0 to be correct
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
